QR1
Async programming was hardest for me because I did not understand
Future and await at first. After testing the code and seeing the delay
between results I understood how async works.

QR2
If we change List<int> to List<double> we must change the list type,
function parameters, and return types. This shows that data types
are important when designing programs.

QR3
In a real Flutter app async is used when fetching data from the
internet. While waiting the app can show a loading spinner.

QR4
Using separate functions like add and subtract makes the program
clearer. A single function with operations could also work but may
be harder to maintain.